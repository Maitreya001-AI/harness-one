/**
 * Adapter contract suite — see `./suite.ts` for the full set of
 * assertions and the record/replay flow.
 *
 * @module
 */

export {
  CONTRACT_FIXTURES,
  cassetteFileName,
  contractFixturesHandle,
  createAdapterContractSuite,
  type AdapterContractSuiteOptions,
  type ContractAdapterFixturesHandle,
  type ContractFixture,
} from './suite.js';

export type { ContractFixtureExpectations } from './fixtures.js';
