# 2026-Q2 Retro · dogfood (inception)

> **This is an inception retro.** The app-level feedback mechanism
> (`HARNESS_LOG.md` + `METRICS.md` + quarterly `RETRO/`) was
> standardized in 2026-Q2 as part of the three-layer architecture
> migration. Earlier dogfood runs predate the standardized artifacts.
>
> Starting 2026-Q3 this directory will host normal quarterly retros.
> See [`docs/harness-one-app-feedback-loop.md`](../../../docs/harness-one-app-feedback-loop.md)
> for the full retro template and gate criteria.

---

## Status at end of Q2

- **Bot status**: running (workflows
  `.github/workflows/dogfood-triage.yml` and `dogfood-weekly.yml`).
- **Reports**: `dogfood-reports/` directory created on first run; not
  yet populated in this checkout.
- **HARNESS_LOG**: standardized this quarter; zero entries to date.
- **METRICS**: standardized this quarter; baseline metrics deferred to
  Q3 once 30 days of runs accumulate.

## What changed in Q2

This quarter's deliverables for the dogfood feedback loop are
**process artifacts**, not bot behavior changes:

- New file: `HARNESS_LOG.md` (template + format contract).
- New file: `METRICS.md` (links + rolling indicators table, baseline
  pending).
- New directory: `RETRO/` with this inception entry.
- Cross-app summary file created at `docs/app-frictions.md` so when
  `coding-agent` or `research-collab` ship, intersecting friction has a
  home.

No code changes to `apps/dogfood/src/`.

## Friction observed this quarter

(None recorded. The standardized log was empty when the quarter ended.
This is recorded honestly here per the
[`harness-one-app-feedback-loop.md`](../../../docs/harness-one-app-feedback-loop.md)
"Honest reporting" section: empty is allowed; absent is not.)

## Impact on harness-one (this quarter)

- Issues opened: 0
- PRs landed: 0
- ADRs / RFCs authored: 0
- Direct doc edits: 0

(No feedback this quarter because the log started empty. Q3 will be
the first quarter with a real signal-to-noise floor.)

## Recommendations to harness-one

(Deferred to Q3 when there is data to base recommendations on.)

## Focus for next quarter (Q3)

1. Fill in 30-day baseline indicators in `METRICS.md` once runs
   accumulate.
2. Triage every `HARNESS_LOG` entry within one week of landing — do not
   let entries pile up.
3. Run the cross-app friction scan procedure
   ([`docs/app-frictions.md`](../../../docs/app-frictions.md)) at end of
   Q3 even if `apps/dogfood/` is still the only running app — the muscle
   memory matters more than the cross-section in Q3.

## Cross-app signals

`apps/dogfood/` is the only app currently running. No cross-app
intersection possible this quarter. Cross-app scan procedure is
documented at [`docs/app-frictions.md`](../../../docs/app-frictions.md)
for when the next app ships.
