name: SBOM

# Generates a CycloneDX SBOM and an npm-audit snapshot on every tagged
# release, and attaches both as assets to the Release. Also runnable via
# workflow_dispatch so maintainers can regenerate an SBOM for an older
# release without cutting a new one.
#
# Why this matters: enterprise procurement teams require a per-release
# SBOM to feed their supply-chain risk tooling. Producing it from CI
# (not a laptop) guarantees the SBOM corresponds to the exact lockfile
# state that produced the release artifacts.

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref (tag or SHA) to SBOM. Defaults to HEAD of the default branch.'
        required: false
        default: ''

permissions:
  contents: read

jobs:
  sbom:
    name: Generate SBOM + audit snapshot
    runs-on: ubuntu-latest
    permissions:
      # `contents: write` is only needed for the "attach to release" step.
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Setup pnpm
        uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e # v6.0.3

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Resolve release version
        id: version
        run: |
          if [ -n "${{ github.event.release.tag_name }}" ]; then
            echo "version=${{ github.event.release.tag_name }}" >> "$GITHUB_OUTPUT"
          else
            version="$(git describe --tags --always)"
            echo "version=${version}" >> "$GITHUB_OUTPUT"
          fi
        shell: bash

      - name: Generate CycloneDX SBOM
        # anchore/sbom-action uses Syft under the hood, which reads the
        # pnpm lockfile and produces a deterministic CycloneDX document.
        uses: anchore/sbom-action@7ccf588e3cf3cc2611714c2eeae48550fbc17552 # v0.15.11
        with:
          format: cyclonedx-json
          output-file: sbom-${{ steps.version.outputs.version }}.json
          # Package everything under the working directory so Syft picks up
          # all workspaces, not just the root manifest.
          path: .
          upload-artifact: false

      - name: Generate npm-audit snapshot
        # Intentionally does NOT set --audit-level — we want the full
        # advisory list in the snapshot, regardless of severity. The CI
        # gate on high/critical lives in audit.yml.
        run: pnpm audit --json > "audit-${{ steps.version.outputs.version }}.json" || true
        shell: bash

      - name: Upload as workflow artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sbom-${{ steps.version.outputs.version }}
          path: |
            sbom-${{ steps.version.outputs.version }}.json
            audit-${{ steps.version.outputs.version }}.json
          if-no-files-found: error

      - name: Attach to GitHub Release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            "sbom-${{ steps.version.outputs.version }}.json" \
            "audit-${{ steps.version.outputs.version }}.json" \
            --clobber
        shell: bash
