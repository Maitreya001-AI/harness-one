---
---

chore(deps): dependency and security maintenance with no consumer-facing
package changes. Covers devDependency bumps (Dependabot), pnpm `overrides`
that force patched transitive versions for open advisories, a ReDoS fix in
the `research-collab` app (unpublished), and CI/lychee config. No published
package's `dependencies`, `peerDependencies`, or runtime behaviour changes,
so this intentionally carries no release.
